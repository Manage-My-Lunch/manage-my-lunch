import { StyleSheet } from "react-native"

const theme = {
    colours: {
        primary: "#00BFA6",
        secondary: "#5faffd",
        grey: "#a1acbd",
        accent: "#a79dff",
    }
}

export default theme
